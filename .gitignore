.venv/
__pycache__/
*.pyc
.env
.pytest_cache/
*.egg-info/
.DS_Store

# Docker — named volumes live under /var/lib/docker and are never in the repo.
# These ignore any local bind-mount data dirs if the compose file is changed to use them.
pgdata/
*_pgdata/
docker-data/
volumes/
.claude/settings.local.json
